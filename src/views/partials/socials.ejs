<table id="social">
    <% for (let [key, value] of Object.entries(links)) { %>
        <tr>
            <th id="<%- key %>"><%- key %></th>
            <% value.forEach(function(info) { %>
                <tr id="<%- info.name.toLowerCase() %>">
                    <a href="<%- info.href %>"><%- info.name %></a>
                </tr>
            <% }); %>
        </tr>
    <% }; %>
</table>